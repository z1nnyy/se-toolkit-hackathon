[project]
name = "cava-bot"
version = "0.1.0"
description = "Telegram bot for browsing the Cava cafe menu"
requires-python = ">=3.9"
dependencies = [
  "aiogram>=3.20.0",
  "httpx>=0.27.0",
  "pydantic-settings>=2.4.0",
]

[tool.uv]
package = false
